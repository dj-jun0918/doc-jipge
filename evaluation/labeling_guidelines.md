# Ground Truth 라벨링 가이드라인 v0.3

**작성**:
- v0.2 초안: 임태규
- v0.2 보강: 방정우
- v0.3 구조화 + 매핑표: 함준규
- v0.3 §5 모호 케이스: 함준규 결정 (2026-06-01)

**버전**: v0.3 — §5 확정 (2026-06-01)
**목적**:
1. 30+ GT 라벨링 기준 통일 (v0.2 목적 유지)
2. **시스템 schema(`ParsedCondition`)와 GT 정렬** → 평가 신뢰도 + 매칭 정확도 측정 가능 (v0.3 신규)
3. **IAA 신뢰도 확보** — 라벨러 일관성 측정 (PR#5 cross-label)
**적용**: 기존 19개 v0.3 재정리 (ann_020 중복 제거) + 신규 라벨링 (총 50 목표)

---

## 0. v0.2 → v0.3 변경 요약

| 변경 | 내용 |
|---|---|
| **+** 모든 필드에 `value` + `operator` 명시 필수 | 시스템 `ParsedCondition` (`backend/app/schemas/eligibility.py:7-11`)과 1:1 정렬 |
| **+** 단위 표준화 | 매출 = 원 단위 정수 (시스템 `Company.revenue: BigInteger` 정렬) |
| **+** 인증 시스템 키 매핑표 (부록 2) | 공고 표기 → 시스템 키 표준 (15개) |
| **+** 매출 단위 변환 보조 표 (부록 3) | 라벨러 오타 방지 |
| **+** `fields: []` 허용 명시 (§6) | 표준 7종 매핑 불가 시 빈 배열 + notes (ann_011 모범 사례) |
| **+** 체크리스트 갱신 (§11) | `value`/`operator` 체크 항목 8개 추가 |
| **+** GT 재정리 | 기존 ann_001~020에 `value`/`operator` 추가 (함준규 일괄 처리) |
| **✓** 모호 케이스 §5 확정 | 9개 케이스 함준규 결정 (2026-06-01). 대원칙: fields[]=7종만, 7종 외→notes |

---

## 1. 파일 구조

```
evaluation/ground_truth/ann_NNN/
└── ground_truth.json
```

### ground_truth.json 필드 명세 (v0.3)

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `announcement_id` | string | ✅ | `"ann_001"` 형식 |
| `title` | string | ✅ | 공고 원문 제목 그대로 |
| `source_url` | string | ✅ | 공고 원문 URL |
| `fields` | array | ✅ | 자격요건 필드 목록 (표준 7종만, 매핑 불가 시 `[]`) |
| `fields[].field_name` | string | ✅ | 표준 7종 중 1개 |
| `fields[].condition` | string | ✅ | 사용자 표시용 텍스트 |
| `fields[].value` | any | ✅ **v0.3 신규** | number / `{min,max}` / string / array / null |
| `fields[].operator` | string | ✅ **v0.3 신규** | 11종 중 1택 (§8 참고) |
| `fields[].evidence` | string | ✅ | 원문 인용 |
| `fields[].evidence_source` | string | ✅ | 페이지 + 항목 위치 |
| `fields[].notes` | string | ❌ | 모호성/매핑 근거 |
| `exclusions` | array | ✅ | 신청제외 조건 (없으면 `[]`) |
| `notes` | string | ❌ | 라벨러 메모, 매핑 불가 조건 기록 |

---

## 2. 표준 7종 필드 정책 (PR#12 확정 + v0.3 유지)

`fields` 배열에는 **아래 7가지 `field_name`만** 사용한다.

| # | field_name | 설명 |
|---|-----------|------|
| 1 | `"업력"` | 창업 후 경과 연수 조건 |
| 2 | `"매출"` | 매출액(또는 매출) 조건 |
| 3 | `"지역"` | 소재지 조건 |
| 4 | `"나이"` | 대표자/신청자 연령 조건 |
| 5 | `"종업원 수"` | 임직원 수 조건 |
| 6 | `"업종"` | 허용/제외 업종 조건 |
| 7 | `"인증"` | 보유 인증 조건 |

### 비표준 자격요건 처리 원칙

| 케이스 | 처리 방법 |
|--------|-----------|
| 표준 7종과 의미가 같은 자격요건 | 표준 필드명으로 매핑. 예: `"스타트업 업력"` → `"업력"` |
| 표준 7종으로 매핑하기 어려운 자격요건 | `fields` 제외, `notes`에 원문과 매핑 불가 사유 기록. 예: `"대표자 국적"` |
| 메타 정보 (사업기간/신청방법/지원한도/제출서류 등) | **자격요건이 아니므로 `fields`에서 완전 제외** |
| 표준 7종 어디에도 매핑 안 되는 공고 | `fields: []` 허용 (§6 참고, ann_011 모범 사례) |

> ⚠️ `fields`는 오직 **"이 공고에 신청하려면 어떤 자격을 갖춰야 하는가"**에 대한 답만 담는다.
> 지원 내용, 사업 일정, 제출 서류, 지원 금액 등은 메타 정보이므로 절대 포함하지 않는다.

---

## 3. 핵심 원칙 (v0.3 신규)

GT의 `condition`은 사용자 표시용 텍스트지만, **시스템과 비교하려면 `value` + `operator`를 함께 명시해야 한다.** 시스템 `ParsedCondition`과 동일 구조.

```python
# backend/app/schemas/eligibility.py:7-11
class ParsedCondition(BaseModel):
    value: float | str | dict | None
    operator: str | None
    raw_text: str
```

### value 형식 요약

| 필드 | value 형식 | 예시 |
|---|---|---|
| 업력 | int 또는 float (년) | `7` |
| 매출 | int (원) | `200000000` (= 2억원) |
| 나이 | int (만 나이) | `39` |
| 종업원 수 | int (명) | `5` |
| 지역 | string 배열 | `["원주"]`, `["강원", "서울"]` |
| 업종 | string 배열 | `["제조업"]`, `["IT", "SW"]` |
| 인증 | string 또는 string 배열 | `"venture_company"`, `["venture_company", "iso_9001"]` |
| 범위 | `{"min": N, "max": M}` | `{"min": 1, "max": 50}` |

### operator 11종

`"미만"` | `"이하"` | `"이상"` | `"초과"` | `"범위"` | `"소재"` | `"무관"` | `"포함"` | `"제외"` | `"보유"` | `"미보유"`

---

## 4. 필드별 라벨링 기준 (v0.3 — value/operator 통합)

### 4-1. 업력

- `field_name`: `"업력"`
- `condition`: 사용자 표시용 텍스트 — `"3년 미만"`, `"7년 이하"`, `"6개월 이상"`, `"창업 후 7년 이내"`
- `value`: 년 단위 int 또는 float
- `operator`: `"미만"` | `"이하"` | `"이상"` | `"초과"` | `"범위"`
- `evidence`: 조건이 명시된 원문 문장 그대로 인용
- `evidence_source`: 페이지 + 항목 위치

**operator 매핑 기준**

| 원문 표현 | operator |
|-----------|----------|
| N년 미만 | `"미만"` |
| N년 이하 | `"이하"` |
| N년 이상 | `"이상"` |
| N년 초과 | `"초과"` |
| N년 이상 M년 이하/미만 | `"범위"` |

> ⚠️ `"미만"`과 `"이하"` 엄격 구분. `"창업 후 3년"` 표현은 문맥상 `"이하"` 또는 `"미만"` 판단하여 `notes`에 근거 기재.
> ⚠️ `condition`은 사용자에게 보여지는 값이므로 `"창업 후 7년 이내인 기업"`처럼 긴 문장보다 `"창업 후 7년 이내"`처럼 조건 중심 표현 권장.

**예시**:
```json
{
  "field_name": "업력",
  "condition": "7년 이하",
  "value": 7,
  "operator": "이하",
  "evidence": "창업 7년 이내 중소기업",
  "evidence_source": "첨부파일 2p 2. 지원대상"
}
```

```json
{
  "field_name": "업력",
  "condition": "1년 이상 7년 이하",
  "value": {"min": 1, "max": 7},
  "operator": "범위",
  "evidence": "창업 1~7년차 기업",
  "evidence_source": "본문 1p"
}
```

### 4-2. 매출

- `field_name`: `"매출"` (원문이 `"매출액"`이어도 **항상 `"매출"`으로 통일** — measure.py `STANDARD_FIELDS`가 `"매출"`만 인식하므로 `"매출액"`은 비표준으로 필터됨)
- `condition`: 사용자 입장에서 기준이 분명한 텍스트
  - 예: `"2억원 이상"`, `"10억원 미만"`, `"1억원 이상 50억원 미만"`
  - 기준 연도/기간이 명시된 경우 그대로 포함:
    `"직전년도(2025) 또는 최근 3개년 연매출 2억원 이상"`
- `value`: **원 단위 정수** (시스템 `Company.revenue: BigInteger` 정렬) — 부록 3 참고
- `operator`: `"미만"` | `"이하"` | `"이상"` | `"초과"` | `"범위"`

**사용자 표시 관점 보완**
- `"2억원 이하"`처럼 금액만 있으면 사용자가 기준 시점을 오해할 수 있으므로, 원문에 기준 연도나 기간이 있으면 반드시 포함
- 화면에 표시될 값임을 고려해 `"직전년도 매출 2억원 이하"`처럼 읽히는 형태 권장

**예시**:
```json
{
  "field_name": "매출",
  "condition": "2억원 이상",
  "value": 200000000,
  "operator": "이상",
  "evidence": "직전년도 매출액 2억원 이상",
  "evidence_source": "첨부파일 3p ③(매출액)"
}
```

```json
{
  "field_name": "매출",
  "condition": "1억원 이상 50억원 미만",
  "value": {"min": 100000000, "max": 4999999999},
  "operator": "범위",
  "evidence": "1억원 이상 50억원 미만",
  "evidence_source": "본문 2p",
  "notes": "원문 '50억원 미만'을 inclusive로 변환 (max: 4999999999)"
}
```

### 4-3. 지역

- `field_name`: `"지역"`
- `condition`: 사용자 입장에서 의미가 명확한 텍스트 — `"원주시 소재"`, `"강원 소재"`, `"전국"`
- `value`: **string 배열** (광역시·도 또는 시·군·구 단위)
  - 광역시·도는 `REGION_GROUPS` 기준 정규화 (`강원특별자치도` → `"강원"`)
  - 시·군·구는 원문 그대로
  - 전국 조건은 `value: null`
- `operator`: `"소재"` (특정 지역) | `"무관"` (전국)

**전국 조건 처리**
- `"전국 소재"`, `"제한 없음"`, `"지역 무관"` → `condition: "전국"`, `value: null`, `operator: "무관"`
- 시스템 matcher는 `raw_text`에 `"전국"` 포함 시 항상 충족 처리

**사용자 표시 관점 보완**
- `condition`은 `"강원 소재"`, `"서울 소재"`처럼 `"소재"` 어휘 포함 권장 (단순 지역명만 쓰면 의미 모호)
- 시·군·구는 원문 그대로 유지하되, 사용자가 거주지 기준인지 사업장 기준인지 오해할 수 있는 경우 `notes`에 명시

**예시**:
```json
{ "field_name": "지역", "condition": "원주시 소재", "value": ["원주시"], "operator": "소재" }
{ "field_name": "지역", "condition": "강원 소재", "value": ["강원"], "operator": "소재" }
{ "field_name": "지역", "condition": "강원 또는 서울 소재", "value": ["강원", "서울"], "operator": "소재" }
{ "field_name": "지역", "condition": "전국", "value": null, "operator": "무관" }
```

> 💡 시·군·구는 원문 그대로 (`"원주시"`), 광역시·도는 정규화 (`"강원"`). 이렇게 분리하면 광역시·도(`["강원"]`)와 시·군·구(`["원주시"]`)가 명확히 구분됨.

### 4-4. 종업원 수

- `field_name`: `"종업원 수"`
- `condition`: 원문 그대로
  - 예: `"5인 미만"`, `"10인 이상 300인 미만"`
  - 업종별 기준 상이: `"광업·제조·건설·운수업 10인 미만 / 그 외 업종 5인 미만"`
- `value`: 정수 또는 `{"min": N, "max": M}` (업종별 복합 조건은 `notes`에 분기 명시 + 대표값만 value로)
- `operator`: `"미만"` | `"이하"` | `"이상"` | `"초과"` | `"범위"`

**예시**:
```json
{
  "field_name": "종업원 수",
  "condition": "10인 이상 300인 미만",
  "value": {"min": 10, "max": 299},
  "operator": "범위",
  "notes": "원문 '300인 미만'을 inclusive로 변환 (max: 299)"
}
```

### 4-5. 나이

- `field_name`: `"나이"`
- `condition`: 만 나이 명시 — `"만 39세 이하"`, `"만 60세 이상"`
- `value`: 만 나이 정수
- `operator`: `"미만"` | `"이하"` | `"이상"` | `"초과"` | `"범위"`
- `"만 나이"` 표현 필수 확인 — `"만"` 없는 경우 `notes`에 명기

**사용자 표시 관점 보완**
- 대표자 기준인지 신청자 기준인지 불명확하면 `notes`에 근거 기재

**예시**:
```json
{
  "field_name": "나이",
  "condition": "만 39세 이하",
  "value": 39,
  "operator": "이하",
  "evidence": "대표자 만 39세 이하인 청년 창업기업",
  "evidence_source": "본문 1p"
}
```

### 4-6. 업종

- `field_name`: `"업종"`
- `condition`: 허용/제외 업종 원문 — 예: `"제조업"`, `"소상공인 정책자금 지원제외 업종 외"`
- `value`: **string 배열** (업종명, 키워드, 또는 KSIC 명)
- `operator`: `"포함"` (허용) | `"제외"` (금지)
- 업종 목록 매우 긴 경우: 대표 3~5개 + 나머지는 `notes`에 출처 기재

**사용자 표시 관점 보완**
- 업종은 공고 원문이 법률 문장처럼 길어질 수 있으므로, 사용자 입장에서 읽히는 수준으로 정리
- 의미를 바꾸지 않는 범위에서만 요약

**예시**:
```json
{
  "field_name": "업종",
  "condition": "제조업",
  "value": ["제조업"],
  "operator": "포함",
  "evidence": "제조업 영위 기업",
  "evidence_source": "본문 1p"
}
```

```json
{
  "field_name": "업종",
  "condition": "소상공인 정책자금 지원제외 업종 외",
  "value": ["유흥주점", "도박업", "사행성"],
  "operator": "제외",
  "notes": "전체 목록: 중소기업창업 지원법 시행령 제4조 (대표 3개만 명시)"
}
```

### 4-7. 인증 (cert_mapping 15종 연동)

- `field_name`: `"인증"`
- `condition`: `"벤처기업 인증 보유"`, `"이노비즈 인증 보유"`
- `value`: **시스템 키 문자열** (단일) 또는 **시스템 키 배열** (복수) — 부록 2 매핑표 참고
- `operator`: `"보유"` (보유 필수) | `"미보유"` (없어야 함)

**사용자 표시 관점 보완**
- `condition`은 `"벤처기업"`처럼 인증명만 적지 말고, `"벤처기업 인증 보유"`처럼 조건 의미가 드러나게 작성
- 여러 인증 중 택일 (OR)이면 `condition`에 그대로 반영
  - 예: `"벤처기업 / 이노비즈 중 1개 이상 보유"`

> ⚠️ **OR 매칭 주의**: 현재 matcher (`backend/app/matcher/matcher.py:204-239`)는 복수 키를 **AND**로 처리. OR 매칭이 필요한 경우 v0.3에서는 `value: null` + `notes`에 원문 표기 + 운영자 토론 후 PR#6에서 matcher OR 지원 추가 검토.

**예시 (AND)**:
```json
{
  "field_name": "인증",
  "condition": "벤처기업 인증 보유",
  "value": "venture_company",
  "operator": "보유",
  "evidence": "벤처기업 인증을 보유한 중소기업"
}
```

**예시 (OR — v0.3에서는 임시 처리)**:
```json
{
  "field_name": "인증",
  "condition": "벤처기업 / 이노비즈 중 1개 이상 보유",
  "value": null,
  "operator": "보유",
  "notes": "OR 매칭 필요 — matcher는 AND만 지원. 원문: '벤처기업 인증 또는 이노비즈 인증 중 하나 보유'. matcher OR 지원 후 value=['venture_company', 'inno_biz']로 변경 예정"
}
```

---

## 5. 모호 케이스 합의 및 확정 (Kappa < 0.6 의사결정 포함)

임태규/방정우 교차 라벨링(Cross-Labeling) 결과 일치도가 낮았던(κ < 0.6) 케이스와 함준규의 v0.3 최종 결정을 바탕으로 정립된 **9가지 확정 룰**입니다.

> **대원칙: `fields[]`에는 표준 7종(업력/매출/지역/나이/종업원 수/업종/인증)만 넣는다.**
> 시스템·평가(measure.py)가 7종만 채점하므로, 7종 외 조건을 fields에 넣으면 채점 및 매칭 점수에 불이익이 발생할 수 있습니다. 7종 외 **자격요건**은 `notes`에 기록하여 정보를 보존하고, 단순 **메타정보**(지원한도/규모)는 제외합니다.

| # | 케이스 | 확정 룰 및 의사결정 내용 |
|---|--------|---------|
| 1 | **자격요건 vs 메타정보** | 7종 자격 → `fields[]` / 7종 외 비표준 자격(예: 신용점수, 대표자 국적 등) → `notes` 기록 / 메타정보(융자한도, 지원금액, 지원규모) → fields에서 제외. |
| 2 | **OR / 복합 조건** | 단일 field 유지 (분리 X). `condition`에 "또는"을 명시하고 `value`는 배열로, `notes`에 "OR(택1)"임을 기록. (예: 벤처 또는 이노비즈) |
| 3 | **트랙별 자격조건** | 트랙들의 7종 매핑 가능 조건만 `fields[]`에 넣되, 구조상 매핑이 불가하면 `fields: []`로 두고 `notes`에 트랙 설명을 기재. |
| 4 | **confidence 필드** | 선택 필드(필수 아님). 라벨러가 불확실할 때만 기록용으로 사용하며, 시스템 평가(`measure.py`)에서는 사용하지 않음. |
| 5 | **operator 필수화** | 적용 완료. 모든 수치 필드(업력/매출/종업원/나이)에 `operator`를 명시하며, 기존 GT도 일괄 재정리함. |
| 6 | **자회사/계열사/모기업** | 신청 주체(공고가 지칭하는 해당 기업) 기준으로만 라벨링. 계열/모기업 자격 관계는 `notes`에 별도로 기록함. |
| 7 | **지역 — 본사 vs 사업장** | 구분 없이 `operator="소재"`로 라벨링 (본사/사업장 중 하나라도 해당 지역이면 충족으로 판정). 구분 필요한 특수한 경우만 `notes` 기재. |
| 8 | **비표준 자격요건** | 대표자 국적, 자본금 등 7종 외 비표준 요건은 `fields`에서 제외하고 `notes`에 원문과 사유 기록. |
| 9 | **"창업 후 N년 이내" 디폴트** | "이내" = "이하" (경계값 포함). `operator="이하"`, `value=N`으로 매핑하여 일관성 확보. |

---

## 6. fields: [] 허용 (v0.3 신규)

표준 7종 매핑이 아예 불가능한 자격요건만 존재하는 공고는 무리하게 매핑하지 않고 `fields: []`를 허용합니다 (ann_011 모범 사례).

- **원칙**: 부정확한 7종 매핑은 전체 평가의 신뢰도를 저해합니다.
- **필수 행동**: `notes`에 매핑 불가 사유 및 실제 원문 자격요건을 정확히 명시해 둡니다.
- **적용 예시 (ann_011)**:
  ```json
  {
    "announcement_id": "ann_011",
    "title": "2026년도 「상생형 AX 선도모델」 주관기관(대기업 등) 모집",
    "fields": [],
    "exclusions": ["스마트제조혁신 지원사업에서 참여제한 중인 기업은 참여 불가"],
    "notes": "신청자격이 '대기업(계열사 포함), 중견기업 등 주관기관'으로 표준 7종 어디에도 매핑되지 않음. 중소기업은 자격 보유 기업과 컨소시엄으로만 참여 가능."
  }
  ```

---

## 7. condition 및 텍스트 작성 규칙 (단위 변환표 통합)

사용자에게 노출되는 `condition` 문장은 공고 원문을 단순 복사하는 것이 아니라, 사용자가 화면에서 보고 즉시 납득할 수 있는 정규화된 형태를 가져야 합니다.

### 7-1. 단위 변환 정규화 가이드라인

원문 텍스트 내 중구난방인 수치 및 물리 단위를 정규화하여 가독성을 극대화합니다.

| 항목 | 원문 표현 예시 | 정규화 가이드라인 | 올바른 condition 예시 |
|------|---------------|--------------------------|-------------------|
| **매출** | `2억`, `2,000만 원`, `200,000,000` | `"억원"`, `"만원"` 단위로 명확히 한글화. 불필요한 쉼표(,) 제거. | `"2억원 이상"`, `"5,000만원 이하"` |
| **업력** | `3년`, `36개월`, `창업 1년 미만` | `"년"`, `"개월"` 표기 통일. 1년 이상은 가급적 년 단위 사용. | `"3년 이하"`, `"6개월 이상"`, `"1년 이상 7년 이하"` |
| **나이** | `만 39세`, `39세 이하`, `청년(만19세~34세)` | 반드시 **`"만"`** 접두어를 명시하여 실제 연령과의 오해 방지. | `"만 39세 이하"`, `"만 19세 이상 만 34세 이하"` |

---

## 8. operator 후보 목록

| operator | 적용 필드 | 설명 |
|----------|-----------|------|
| `"미만"` | 업력, 매출, 종업원 수, 나이 | 미만 (경계값 미포함) |
| `"이하"` | 업력, 매출, 종업원 수, 나이 | 이하 (경계값 포함) |
| `"이상"` | 업력, 매출, 종업원 수, 나이 | 이상 (경계값 포함) |
| `"초과"` | 업력, 매출, 종업원 수, 나이 | 초과 (경계값 미포함) |
| `"범위"` | 업력, 매출, 종업원 수, 나이 | N 이상 M 이하/미만, value: `{"min", "max"}` |
| `"소재"` | 지역 | 특정 지역 소재 |
| `"무관"` | 지역 | 전국, 지역 제한 없음 |
| `"포함"` | 업종 | 허용 업종 목록 |
| `"제외"` | 업종 | 금지 업종 목록 |
| `"보유"` | 인증 | 인증 보유 필수 |
| `"미보유"` | 인증 | 인증 없어야 함 |

---

## 9. v0.3 형식 예시 보강 (structured JSON - 5가지 표준 필드 예시)

값(value)과 연산자(operator)의 구체적인 결합 및 증거 원문(evidence)이 어떻게 구조화되는지 보여주는 **5종 표준 필드 모범 예시**입니다.

```json
{
  "announcement_id": "ann_999",
  "title": "2026년도 혁신분야 스타트업 성장지원 공고",
  "source_url": "https://www.example.go.kr/announcement/999",
  "fields": [
    {
      "field_name": "업력",
      "condition": "창업 후 7년 이내",
      "value": 7,
      "operator": "이하",
      "evidence": "창업 7년 이내 창업기업만 신청 가능하다고 명시됨 (2019-04-29 ~ 2026-04-28 설립)",
      "evidence_source": "공고문 2쪽, 5쪽"
    },
    {
      "field_name": "매출",
      "condition": "직전년도(2025) 매출 2억원 이상",
      "value": 200000000,
      "operator": "이상",
      "evidence": "직전 사업연도(2025년) 매출액이 2억원 이상인 기업",
      "evidence_source": "공고문 3쪽 지원 대상"
    },
    {
      "field_name": "지역",
      "condition": "대구 소재",
      "value": ["대구"],
      "operator": "소재",
      "evidence": "공고일 현재 대구광역시 내에 본사 또는 공장을 등록한 기업",
      "evidence_source": "공고문 1쪽 신청 자격"
    },
    {
      "field_name": "나이",
      "condition": "대표자 만 39세 이하",
      "value": 39,
      "operator": "이하",
      "evidence": "대표자가 만 39세 이하의 청년 창업자여야 함 (1986년 5월 31일 이후 출생자)",
      "evidence_source": "공고문 2쪽 대표자 연령"
    },
    {
      "field_name": "인증",
      "condition": "벤처기업 인증 보유",
      "value": "venture_company",
      "operator": "보유",
      "evidence": "신청 접수 마감일 기준 벤처기업 육성에 관한 특별조치법에 따른 벤처기업 확인서 보유 기업",
      "evidence_source": "공고문 4쪽 우대/필수 서류"
    }
  ],
  "exclusions": [
    "금융기관 채무불이행 규제 중인 기업",
    "국세 또는 지방세 체납 기업",
    "휴·폐업 중인 기업"
  ],
  "notes": "스타트업 업력과 매출, 지역 등의 5종 기본 조건을 추출하여 기재함. 대표자 연령은 공고문 기준 만 39세 이하로 정규화함."
}
```

---

## 10. confidence / notes 사용 기준

**사용자 화면에 표시했을 때 의미가 모호해질 수 있는 조건**은 반드시 `notes`에 근거를 남겨 정성적 분석을 보조합니다.

### notes에 기재해야 할 주요 케이스
- 조건이 본문이 아닌 첨부파일·별표에만 있는 경우
- `"창업 후 N년"` 표현이 `"미만"`인지 `"이하"`인지 해석 여지가 있어 합의 룰을 적용한 경우
- 업종 조건이 포함/제외 목록이 혼재되어 정리가 요구되는 경우
- 수치 조건의 기준일(공고일/접수일/신청일)이 모호하거나 변칙적일 경우
- 복수 조건이 AND인지 OR인지 불명확한 경우
- 범위(`operator: "범위"`)의 inclusive 변환을 라벨러가 대행하여 기록한 경우 (예: `max: N-1` 처리 명시)
- OR 인증 매칭 케이스 (`value: null`로 기록하고 원문을 notes에 기입)

---

## 11. exclusions 작성 규칙

- 원문 의미를 유지하되, **사용자가 읽었을 때 바로 제외 사유를 이해할 수 있도록** 간결하게 작성합니다.
- 한 문장에 여러 조건을 과하게 묶지 말고 항목별로 한 줄씩 분리합니다.
- 일반적인 공통 제외 조건(체납, 휴폐업, 채무불이행)은 반드시 exclusions 배열에 추가합니다.

---

## 12. 라벨링 체크리스트 (v0.2 + v0.3 통합 개인 검수용)

- [ ] `title`이 공고 원문 제목과 정확히 일치하는가
- [ ] `source_url`이 실제 작동하는 정상 링크인가
- [ ] `fields`에 표준 7종 외 필드명이 사용되지 않았는가
- [ ] 메타 정보(사업기간/신청방법/지원한도/제출서류 등)가 `fields`에서 완전히 제외되었는가
- [ ] 모든 수치 조건에 `operator`가 명확히 매핑되는가
- [ ] 지역명이 `REGION_GROUPS` 기준으로 정규화되었는가
- [ ] `exclusions`에 휴폐업/체납/채무불이행 조건이 한 줄씩 깔끔히 정리되었는가
- [ ] 모든 수치 필드(업력/매출/나이/종업원)에 `value`(숫자) + `operator` 명시되었는가
- [ ] 매출의 `value`는 **원 단위 정수** (예: `200000000`)로 올바르게 0의 개수가 입력되었는가
- [ ] 범위 operator는 `value: {"min": N, "max": M}` 형식인가
- [ ] 지역의 `value`는 **배열** 형식 (`["원주"]`)인가
- [ ] 업종의 `value`는 **배열** + `operator`가 `"포함"` 또는 `"제외"`인가
- [ ] 인증의 `value`는 매핑표(부록 2)의 시스템 키 형식인가

---

## 부록 1 — ground_truth.json 스키마 (v0.3)

```json
{
  "announcement_id": "ann_NNN",
  "title": "...",
  "source_url": "...",
  "fields": [
    {
      "field_name": "업력|매출|지역|나이|종업원 수|업종|인증",
      "condition": "사용자 표시용 텍스트",
      "value": null,
      "operator": "미만|이하|이상|초과|범위|소재|무관|포함|제외|보유|미보유",
      "evidence": "원문 인용",
      "evidence_source": "위치 표기",
      "notes": "선택 — 변환 근거나 모호성 기록"
    }
  ],
  "exclusions": [
    "휴·폐업 중인 소상공인",
    "국세나 지방세를 체납 중인 소상공인"
  ],
  "notes": "선택 — 라벨러 전체 메모, 매핑 불가 조건 기록 등"
}
```

---

## 부록 2 — 인증 표준 키 매핑표 (cert_mapping.py 기준 15종)

공고 인증 표기 → 표준 cert 키. **`backend/app/matcher/cert_mapping.py`의 `CERT_MAPPING`과 1:1로 완벽히 대응**합니다.

| 표준 키 (Standard Key) | 공고 표기 한글 키워드 목록 (Keywords) |
|---|---|
| `venture_company` | 벤처기업, 벤처기업 인증, 벤처기업 확인서 |
| `inno_biz` | 이노비즈, 기술혁신형 중소기업 |
| `main_biz` | 메인비즈, 경영혁신형 중소기업 |
| `iso_9001` | ISO 9001, 품질경영시스템 |
| `iso_14001` | ISO 14001, 환경경영시스템 |
| `iso_27001` | ISO 27001, 정보보안경영시스템 |
| `iso_22000` | ISO 22000, 식품안전경영시스템 |
| `gmp` | GMP, 우수의약품제조관리기준 |
| `haccp` | HACCP, 식품안전관리인증 |
| `ce_marking` | CE, CE 마킹 |
| `kc_certification` | KC, KC 인증 |
| `women_owned` | 여성기업, 여성기업확인서 |
| `social_enterprise` | 사회적기업, 사회적기업 인증 |
| `rd_lab` | 기업부설연구소 |
| `ip_protection` | 특허, 지식재산권, 실용신안 |

---

## 부록 3 — 매출 단위 변환 보조 표 (라벨러 참고용)

공고 원문 → `value` (원 단위 정수). 라벨러 0 개수 세기 오타 방지용 보조 테이블입니다.

| 공고 표기 | value (원) |
|---|---|
| 1천만원 | `10000000` |
| 5천만원 | `50000000` |
| 1,200만원 | `12000000` |
| 1억원 | `100000000` |
| 2억원 | `200000000` |
| 5억원 | `500000000` |
| 10억원 | `1000000000` |
| 50억원 | `5000000000` |
| 100억원 | `10000000000` |
| 500억원 | `50000000000` |
| 1000억원 | `100000000000` |
